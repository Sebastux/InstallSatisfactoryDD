---
ansible_managed: "Ce fichier est géré par ansible. Toute modification de celui-ci sera définitivement perdu à la prochaine exécution du playbook sur cette machine."

installe_packages: ['chrony', 'vim', 'python3-pip', 'python3', 'vim-doc', 'vim-scripts', 'vim-syntastic',
                    'fail2ban', 'firewalld', 'locate', 'btop', 'bpytop', 'htop', 'iotop']

steam_packages: ['steamcmd:i386']

# Liste des serveurs NTP pour chrony.
liste_serveur:
  - 0.fr.pool.ntp.org iburst
  - 1.fr.pool.ntp.org iburst
  - 2.fr.pool.ntp.org iburst
  - 3.fr.pool.ntp.org iburst

seq: ""